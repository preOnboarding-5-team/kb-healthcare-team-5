// export const getComputedFontSize = (fontSize: number | string) => {
//   if (typeof fontSize === 'number') return fontSize;

//   const l = fontSize.length;

//   if (fontSize.substring(l - 3) === 'rem')
// }

export {};
